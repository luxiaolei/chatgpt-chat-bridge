{
  "name": "chatgpt-chat-bridge",
  "version": "0.2.0",
  "private": false,
  "description": "ChatGPT Project chat routing and orchestration over Ego Lite's native ego-browser runtime.",
  "license": "MIT",
  "scripts": {
    "check": "node --check src/main.js && zsh -n bin/chat-bridge scripts/install.sh scripts/uninstall.sh",
    "test": "zsh tests/static.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
